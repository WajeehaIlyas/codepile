// Single-line comment
/* Multi-line
   comment should be ignored */

int main() {
    int a = 10;
    float b = 3.14;
    string s = "Hello\nWorld\t!";
    bool flag = true;
    bool eqTest = (a == 10) && (b != 0.0);

    a += 5;
    b -= 2.5;
    a++;
    b--;

    if (a >= b && !(flag || false)) {
        return 1;
    } else {
        return 0;
    }

    // Edge cases:
    int abc = 42;  // invalid identifier (digit + letters)
}
