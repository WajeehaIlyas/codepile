//example 1
int a(int x, int y) { return x + y; }

int b = a(3, 6);
int f = 1;
int z = 2;
int h = 6 * f + 3 * 2 - b * b * z * z;
int c = 10 - (-b * 10 + 12) + (5 - 10);

int find_first_even(int start) {
    while (start < 100) {
        if (start % 2 == 0) {
            return start;
        } else {
            start = start + 1;
        }
    }
    return -1;
}
